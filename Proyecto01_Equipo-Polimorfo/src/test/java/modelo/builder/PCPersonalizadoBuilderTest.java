package modelo.builder;

import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.Test;

import modelo.componente.CPU;
import modelo.componente.Disco;
import modelo.componente.FuenteAlimentacion;
import modelo.componente.GPU;
import modelo.componente.Gabinete;
import modelo.componente.MotherBoard;
import modelo.componente.RAM;
import modelo.computadora.ComputadoraBase;
import modelo.computadora.ComputadoraBasica;

/**
 * Clase de pruebas unitarias para PCPersonalizadoBuilder.
 * Valida la correcta construccion de computadoras personalizadas utilizando el patron Builder
 * con factories especificas (Intel o AMD).
 *
 * <p>Esta suite de pruebas esta pendiente de implementacion completa.
 * Se deben agregar pruebas para validar:
 * <ul>
 *   <li>Construccion de PCs con IntelFactory</li>
 *   <li>Construccion de PCs con AMDFactory</li>
 *   <li>Seleccion correcta de componentes compatibles</li>
 *   <li>Integracion con el patron Abstract Factory</li>
 * </ul>
 *
 * @author Equipo-Polimorfo
 * @version 1.0
 */
class PCPersonalizadoBuilderTest{

}
